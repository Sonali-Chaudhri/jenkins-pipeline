pipeline {
    agent {
        docker { image 'node:16-alpine' }
    }
    stages {
        stage('Run Container') {
            steps {
                sh 'docker run -d --name test-container node:16-alpine'
            }
        }
    }
}
